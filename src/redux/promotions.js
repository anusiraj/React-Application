import { PROMOTIONS } from '../shared/promotions';

export const Promotions = (state = PROMOTIONS, action )  => {
    // state function contains Cooments and actions as parameters

    switch(action.type) {
        default:
            return state;
    }

}